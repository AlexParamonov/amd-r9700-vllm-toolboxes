# Manual Test Results: fp8-gemm-bench

**Date:** 2026-06-12
**Tester:** Orchestrator (direct SSH to localai, docker exec into vllm-r9700)

## Test Results

| # | Criterion | Result | Notes |
|---|-----------|--------|-------|
| 1 | Script runs without errors | ✅ PASS | Completed in ~24 min with 256-config search space |
| 2 | All 4 (N,K) configs generated | ✅ PASS | N=5120,K=3072; N=5120,K=8704; N=7168,K=5120; N=17408,K=5120 |
| 3 | Valid JSON in configs dir | ✅ PASS | All 4 files in `/opt/venv/.../configs/` |
| 4 | Config format matches MI300X | ✅ PASS | Keys: BLOCK_SIZE_M/N/K, GROUP_SIZE_M, num_warps, kpack, matrix_instr_nonkdim |
| 5 | No sub-optimal config warnings | ⚠️ UNVERIFIED | vLLM startup takes 5+ min; couldn't capture startup logs cleanly before needing to kill |
| 6 | Idempotent (skips existing) | ✅ PASS | Second dry-run showed `[EXISTS]` for all 4 shapes |
| 7 | No secrets in code | ✅ PASS | Clean |

## What was fixed during testing

1. **Search space too large** (5120 → 256 configs): Removed `num_stages` (not in output format), fixed `matrix_instr_nonkdim=[16]` and `block_k=[128]` to match MI300X pattern. Runtime dropped from estimated 8+ hours to 24 minutes.

2. **Tests updated** to match reduced search space. 39/39 pass.

## Unverified criterion

AC #5 ("After running the benchmark and restarting vLLM, the 'sub-optimal config' warnings no longer appear") requires:
- vLLM to fully load the model (Qwen3.6-27B-FP8, ~5 min startup)
- Capture stderr during startup to check for warnings
- This was blocked by: difficulty capturing logs from detached docker exec, and the need to keep the machine available

**Recommendation:** Verify AC #5 manually next time vLLM is started normally. The configs are in the right location with the right format, so warnings should be gone.

## Notes

- Untrusted config `N=8192,K=5120` exists in the configs dir (copy-paste from another GPU). Not generated by our benchmark. Documented in BENCH_RESULTS.md.
- kpack=2 wins at small batch sizes, kpack=1 at larger sizes. R9700 prefers smaller BLOCK_SIZE_M than MI300X.
